# Production uses durable Postgres storage. Never commit the real value.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require

# Local SQLite development remains available to the original API.
AFFILIATE_INTEL_DB=affiliate_intel.db
